# CNN-LSTM-Pump-Failure
This project implements a hybrid parallel CNN-LSTM model for predictive maintenance in pump systems. The CNN extracts spatial features from sensor data, while the LSTM captures temporal dependencies, enabling accurate failure prediction.
